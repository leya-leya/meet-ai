# AGENTS.md

## 1. 项目目标

本项目名称暂定为 **AI 智能会议纪要与内容管理系统 V1.0**。

V1.0 的唯一目标是完成以下闭环：

> 上传音视频 → 转文字 → AI 摘要 → 内容编辑 → 历史记录 → 导出

这是一个供公司内部使用的最小可用产品（MVP），同时需要保留后续软件著作权登记所需的代码、文档、界面截图和版本记录。

**优先级顺序：**

1. 能稳定运行。
2. 六项核心功能全部闭环。
3. 代码结构清晰、容易说明。
4. 可以测试和复现。
5. 方便整理软著材料。
6. 最后才考虑界面美观和额外体验。

如果“让产品更完整”和“尽快完成 V1.0”发生冲突，永远选择后者。

---

## 2. 范围锁定

除非用户明确修改 `docs/PRODUCT_SPEC.md`，否则不得新增 V1.0 范围之外的产品功能。

### V1.0 必须完成

- 上传单个音频或视频文件
- 创建处理记录
- 调用 ASR 服务完成语音转文字
- 调用大模型完成 AI 摘要
- 编辑并保存摘要
- 编辑并保存完整转写文本
- 查看历史记录
- 按标题搜索历史记录
- 查看单条记录详情
- 删除单条历史记录及其本地文件
- 导出 TXT
- 导出 Markdown

### V1.0 明确禁止

不得擅自实现以下功能：

- 多租户
- 会员系统
- 注册系统
- 微信登录
- 手机验证码
- 支付
- 云存储
- 微服务
- Docker
- Kubernetes
- Redis
- 消息队列
- WebSocket
- 实时会议转写
- 多人协作
- 复杂权限管理
- 原生 App
- 小程序
- 自动剪视频
- 自动发布到视频号/抖音
- AI 聊天机器人
- 知识库
- RAG
- 向量数据库
- PDF 复杂导出
- Word 复杂导出
- OCR
- 多语言翻译
- 自动生成 PPT
- 用户画像
- 数据看板
- 计费系统

如果开发过程中发现某项额外功能“很容易顺手做”，也不得加入。

---

## 3. 固定技术栈

除非出现明确的技术阻塞，不得主动更换技术栈。

### 前端

- React
- Vite
- TypeScript
- 原生 CSS 或少量简单 CSS
- `fetch` 调用后端 API

### 后端

- Python
- FastAPI
- SQLAlchemy
- SQLite
- Pydantic

### 文件存储

- 本地目录 `uploads/`

### AI 能力

- ASR：通过统一适配器调用第三方语音识别服务
- LLM：通过统一适配器调用大模型 API

业务代码不得直接依赖某一家厂商 SDK 的具体对象。

---

## 4. 开发原则

### 4.1 先读文档再写代码

开始任何任务前，至少读取：

1. `AGENTS.md`
2. `docs/PRODUCT_SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/DEVELOPMENT_PLAN.md`

涉及测试时再读取：

- `docs/TESTING.md`

涉及 AI 时再读取：

- `docs/AI_INTEGRATION.md`

涉及软著材料和版本时再读取：

- `docs/SOFT_COPYRIGHT.md`

---

### 4.2 一次只做一个开发任务

一次只完成 `docs/DEVELOPMENT_PLAN.md` 中的一项任务。

不得因为当前任务涉及某个模块，就顺带重构其他无关模块。

不得一次性重写整个项目。

---

### 4.3 修改前先检查现状

修改代码前：

1. 查看相关文件。
2. 确认已有接口和数据结构。
3. 优先沿用现有结构。
4. 只修改完成当前任务所需的最少文件。

---

### 4.4 测试优先

新增业务逻辑时，优先采用以下顺序：

1. 写或补充测试。
2. 运行测试，确认测试能发现缺失行为。
3. 写最小实现。
4. 再运行测试。
5. 运行与当前功能相关的完整测试。

不要为了“测试覆盖率好看”写无意义测试。

---

### 4.5 不隐藏失败

以下情况不得静默失败：

- 上传失败
- 文件格式不支持
- ASR 调用失败
- LLM 调用失败
- 数据库保存失败
- 导出失败
- 文件不存在

后端应返回明确错误信息；前端应展示用户能理解的提示。

不得伪造“转写成功”或“AI 摘要成功”。

---

## 5. 代码边界

### 前端负责

- 页面展示
- 文件选择
- 用户操作
- 调用后端 API
- 编辑摘要和转写文本
- 下载导出文件
- 展示错误和处理状态

### 后端负责

- 文件校验
- 文件保存
- 数据持久化
- 调用 ASR
- 调用 LLM
- 生成导出内容
- 删除文件和记录
- 提供历史记录接口

### AI 适配层负责

- 把业务请求转换为厂商 API 请求
- 把厂商响应转换为本项目统一结果
- 把厂商错误转换为统一异常

业务层不得散落厂商 API 细节。

---

## 6. 数据安全与密钥

严禁：

- 将 API Key 提交到 Git
- 将真实密钥写入源代码
- 将真实密钥写入 README
- 在日志中完整输出密钥
- 在前端暴露后端大模型密钥

统一通过环境变量读取。

仓库只允许提交 `.env.example`，不得提交 `.env`。

---

## 7. 文件处理约束

V1.0 支持的扩展名：

- `.mp3`
- `.wav`
- `.m4a`
- `.mp4`
- `.mov`

默认单文件最大限制：`500 MB`。

上传文件必须重命名为内部唯一文件名，避免同名覆盖。

数据库保留原始文件名用于展示。

---

## 8. 状态约束

一条记录的 `status` 只能使用：

- `uploaded`
- `transcribing`
- `transcribed`
- `summarizing`
- `completed`
- `failed`

不得擅自新增状态。

如果处理失败：

- 状态设为 `failed`
- 保存简短错误信息到 `error_message`
- 不删除已经生成的有效转写或摘要内容

---

## 9. API 兼容约束

新增或修改 API 前先检查 `docs/ARCHITECTURE.md`。

不得随意改字段名。

不得为了当前一个页面方便，破坏已有 API。

如果确实需要变更 API：

1. 先更新 `docs/ARCHITECTURE.md`
2. 再修改测试
3. 再修改后端
4. 最后修改前端

---

## 10. UI 原则

V1.0 只允许三个主页面：

1. 上传页
2. 历史记录页
3. 记录详情页

UI 要求：

- 清晰
- 简洁
- 可截图
- 不追求复杂动画
- 不追求设计系统
- 不引入重量级 UI 框架，除非已有项目已经使用

---

## 11. 完成定义

一个任务只有在以下条件全部满足时才能标记完成：

- 功能实际可用
- 当前任务相关测试通过
- 没有新增明显控制台报错
- 没有把 API Key 写进代码
- 没有实现范围外功能
- 相关文档与实际实现一致
- 如有用户可见页面变化，保留一张截图到 `docs/screenshots/`
- 在 `README.md` 的版本记录中增加一条简短记录

不能以“代码已经写完”作为完成标准。

---

## 12. Codex 每次执行任务时的输出格式

完成任务后，只报告：

### 完成内容
- 修改了什么

### 验证结果
- 执行了哪些测试/命令
- 结果是否通过

### 变更文件
- 文件列表

### 下一步
- 只指出 `docs/DEVELOPMENT_PLAN.md` 中下一项尚未完成的任务

不要主动提出范围外功能。
不要生成“未来可以加入几十个功能”的建议列表。
